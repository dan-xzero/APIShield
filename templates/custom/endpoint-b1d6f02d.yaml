id: endpoint-b1d6f02d
info:
  author: APIShield
  description: Security scan for GET /api/warehouse/{locationId}/manifests/send
  name: Endpoint Scan - GET /api/warehouse/{locationId}/manifests/send
  severity: info
  tags:
  - api
  - endpoint
  - get
requests:
- headers:
    Accept: application/json
    Authorization: Basic {{API_AUTHORIZATION_HEADER}}
    Content-Type: application/json
  matchers:
  - condition: or
    part: body
    type: word
    words:
    - error
    - exception
    - sql
    - mysql
    - postgresql
    - oracle
    - syntax error
    - stack trace
  - condition: or
    status:
    - 500
    - 502
    - 503
    - 504
    type: status
  - condition: or
    part: body
    regex:
    - (?i)(sql|mysql|postgresql|oracle).*error
    - (?i)(syntax|parse).*error
    type: regex
  matchers-condition: or
  method: GET
  path:
  - '{{BaseURL}}/api/warehouse/{locationId}/manifests/send'
