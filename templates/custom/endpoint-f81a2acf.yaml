id: endpoint-f81a2acf
info:
  author: APIShield
  description: Security scan for POST /inventory-units/allocate
  name: Endpoint Scan - POST /inventory-units/allocate
  severity: info
  tags:
  - api
  - endpoint
  - post
requests:
- body: '{"request_body": {"orderId": "123456", "productId": "789", "quantity": 5,
    "locationId": "warehouseA", "userId": "user123", "securityToken": "abc123xyz"}}'
  headers:
    Accept: application/json
    Authorization: Basic {{API_AUTHORIZATION_HEADER}}
    Content-Type: application/json
  matchers:
  - condition: or
    part: body
    type: word
    words:
    - error
    - exception
    - sql
    - mysql
    - postgresql
    - oracle
    - syntax error
    - stack trace
  - condition: or
    status:
    - 500
    - 502
    - 503
    - 504
    type: status
  - condition: or
    part: body
    regex:
    - (?i)(sql|mysql|postgresql|oracle).*error
    - (?i)(syntax|parse).*error
    type: regex
  matchers-condition: or
  method: POST
  path:
  - '{{BaseURL}}/inventory-units/allocate'
