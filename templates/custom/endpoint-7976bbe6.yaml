id: endpoint-7976bbe6
info:
  author: APIShield
  description: Security scan for POST /v1/adhoc/dispatch-warehouse-events
  name: Endpoint Scan - POST /v1/adhoc/dispatch-warehouse-events
  severity: info
  tags:
  - api
  - endpoint
  - post
requests:
- body: '{"request_body": {"items": [123, 456, 789]}}'
  headers:
    Accept: application/json
    Authorization: Basic {{API_AUTHORIZATION_HEADER}}
    Content-Type: application/json
  matchers:
  - condition: or
    part: body
    type: word
    words:
    - error
    - exception
    - sql
    - mysql
    - postgresql
    - oracle
    - syntax error
    - stack trace
  - condition: or
    status:
    - 500
    - 502
    - 503
    - 504
    type: status
  - condition: or
    part: body
    regex:
    - (?i)(sql|mysql|postgresql|oracle).*error
    - (?i)(syntax|parse).*error
    type: regex
  matchers-condition: or
  method: POST
  path:
  - '{{BaseURL}}/v1/adhoc/dispatch-warehouse-events'
