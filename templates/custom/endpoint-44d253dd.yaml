id: endpoint-44d253dd
info:
  author: APIShield
  description: Security scan for PUT /inventory-bulk-adjustment
  name: Endpoint Scan - PUT /inventory-bulk-adjustment
  severity: info
  tags:
  - api
  - endpoint
  - put
requests:
- body: '{"request_body": {"adjustmentType": "increase", "adjustmentReason": "restocking",
    "adjustmentDetails": [{"productId": "ABC123", "locationId": "WH001", "quantity":
    100}, {"productId": "XYZ789", "locationId": "WH002", "quantity": 50}]}}'
  headers:
    Accept: application/json
    Authorization: Basic {{API_AUTHORIZATION_HEADER}}
    Content-Type: application/json
  matchers:
  - condition: or
    part: body
    type: word
    words:
    - error
    - exception
    - sql
    - mysql
    - postgresql
    - oracle
    - syntax error
    - stack trace
  - condition: or
    status:
    - 500
    - 502
    - 503
    - 504
    type: status
  - condition: or
    part: body
    regex:
    - (?i)(sql|mysql|postgresql|oracle).*error
    - (?i)(syntax|parse).*error
    type: regex
  matchers-condition: or
  method: PUT
  path:
  - '{{BaseURL}}/inventory-bulk-adjustment'
